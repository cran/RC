RC.ls.module <-
function(module="Kernel Density Estimation") {
  stop("Not yet available.")
  message("This function will change in the near future.")
  r <- RC.ls(keyword=module, quote=F, module=module)
  return(r)
}

